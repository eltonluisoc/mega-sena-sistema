# Build artifacts do PyInstaller (gerados a partir de bolao_pro_v3.py)
build/
dist/
*.spec
__pycache__/
*.pyc

# Banco de dados local e backups — dados financeiros e pessoais reais
# de participantes. Achado na Rodada 57 que isso nunca esteve no
# .gitignore (o repositório é público — ver Rodada 14).
*.db
backups/

# Config local do Claude Code
.claude/
